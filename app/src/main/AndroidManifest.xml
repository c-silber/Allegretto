<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="goldie.allegretto" >

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <meta-data
            android:name="goldie.allegretto.CLIENT_ID"
            android:value="2289RB"/>
        <meta-data
            android:name="goldie.allegretto.REDIRECT_URL"
            android:value="https://fitauth"/>
        <activity
            android:name=".Login"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="goldie.allegretto.Register"
            android:label="@string/app_name" />
        <activity
            android:name="goldie.allegretto.MusicPref"
            android:label="@string/app_name" />
        <activity
            android:name="goldie.allegretto.Music"
            android:label="@string/app_name" />
        <activity
            android:name="goldie.allegretto.Auth"
            android:label="@string/app_name" />
        <activity
            android:name="com.spotify.sdk.android.authentication.LoginActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"/>
        <activity
            android:name="goldie.allegretto.HealthProfile" >

        </activity>
        <activity
            android:name="goldie.allegretto.Complete"
            android:label="@string/app_name" /><!-- ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information. -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>